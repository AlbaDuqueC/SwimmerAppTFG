<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_pantalla"
    android:orientation="vertical">

    <!-- ============== BARRA SUPERIOR ============== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/altura_header"
        android:background="@color/bg_header"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="@dimen/espacio_m"
        android:paddingEnd="@dimen/espacio_m">

        <!-- Foto de perfil del usuario (siempre visible) -->
        <ImageButton
            android:id="@+id/btnTopPerfil"
            android:layout_width="@dimen/icono_perfil_top"
            android:layout_height="@dimen/icono_perfil_top"
            android:background="@drawable/bg_avatar_circle"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:src="@drawable/usuario"
            android:contentDescription="Ir al Perfil"
            app:tint="@color/blanco"/>

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1"/>

        <!-- Flecha atrás (solo visible en pantallas hijas) -->
        <ImageButton
            android:id="@+id/btnAtras"
            android:layout_width="@dimen/icono_boton_atras"
            android:layout_height="@dimen/icono_boton_atras"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Volver atrás"
            android:visibility="gone"/>

    </LinearLayout>

    <!-- ============== CONTENIDO ============== -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph"/>

    <!-- ============== BARRA INFERIOR ============== -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/altura_bottom_nav"
        android:background="@color/bg_bottom_nav"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        app:itemIconSize="@dimen/icono_nav"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"/>

</LinearLayout>